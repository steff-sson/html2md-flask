name: Build and Push Docker Image

on:
  push:
    branches: [ main ]
    paths:  # NUR bei diesen Dateien
      - 'app/**'
      - 'run.py'
      - 'requirements.txt'
      - 'Dockerfile'
      - '.github/workflows/docker-build.yml'
  workflow_dispatch:  # Manueller Trigger bleibt

jobs:
  build:
    # ... rest bleibt gleich